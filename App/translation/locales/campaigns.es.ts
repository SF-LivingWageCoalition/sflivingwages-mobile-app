/**
 * Campaign copy (Spanish). Same structure as campaigns.en.
 * TODO: Replace English strings with Spanish translations.
 */
import campaignsEn from "./campaigns.en";

const campaignsEs = { ...campaignsEn };

export default campaignsEs;
